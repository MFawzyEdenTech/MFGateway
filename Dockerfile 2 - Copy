FROM node:12-alpine
WORKDIR /my-app
COPY . .
RUN npm install
EXPOSE 3000
#expose used for notifing the reader and also used if you use docker run -P
CMD ["node", "server.js"]
#cmd is used as node server.js als
# CMD 9"node", "-f", "server.js" means node -f server.js